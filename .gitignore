.DS_Store
node_modules/
.next/
.env.local
.env*.local
*.mp4
*.webm
# Keep only the web-delivery hero clip derivatives and their source masters.
!public/video/hero-mccarren-1080.mp4
!public/video/hero-mccarren-1080.webm
!assets-src/video/hero-mccarren-master.mp4
!assets-src/video/hero-mccarren-master.webm
.vercel
tsconfig.tsbuildinfo

# Runtime asset audit output (scripts/audit-public-assets.mjs) — machine-
# readable evidence, regenerate as needed, not shipped or reviewed as source.
/evidence/

# Local AI/editor workflow files (keep untracked)
.agents/
.claude/
skills-lock.json

# Raw founder photo sources (only optimized public/img/** is committed)
updated_images/
ignore/

# Test output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Generated by `next dev` (Next 16 agent rules); local tooling, not project source.
/AGENTS.md
/CLAUDE.md

# Firebase emulator debug logs
*-debug.log
firebase-debug.log
firestore-debug.log
storage-debug.log
ui-debug.log

# Bundle-analyzer evidence (plans/010 P0/P3): local before/after records, not
# project source.
/evidence/bundle/
